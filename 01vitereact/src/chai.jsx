function Chai(){
    return(
        <>
        {/* //using this (fragment) you can technically transfer multiple element */}
        <h3>Chai is ready</h3>
        <h1>This is the seond element</h1>
        <h4>only one element can be transferd once so we are transfering only one element using fragment inside that element there is more than one element</h4>

        </>
    )
} 

export default Chai